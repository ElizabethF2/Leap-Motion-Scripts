# Leap Motion Scripts

This is a collection of old scripts which enable using a Leap Motion sensor as a controller for various games. To use these scripts, you must have Python and the Python packages `websocket-client` and `autopy` installed and the Leap Motion service must be running.
